transfer-service-api {
  port = "8001"
}

consignment-api {
  url = "http://localhost:9001/graphql"
  url = ${?CONSIGNMENT_API_URL}
}

auth {
  url = "http://localhost:8000/auth"
  realm = "tdr"
}

s-3 {
  metadata-upload-bucket = "s3BucketNameMetadata"
  records-upload-bucket = "s3BucketNameRecords"
}